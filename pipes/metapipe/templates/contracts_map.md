# contracts_map.md

## Stage Output Contract Mapping

- align_requirements
  - requirements.md -> markdown header contract
  - acceptance_criteria.md -> markdown header contract
  - constraints.md -> markdown header contract

- design_framework
  - workflow.yaml -> YAML required keys contract
  - roles.yaml -> YAML required keys contract

- plan_tasks
  - tasks.yaml -> YAML required keys contract

- finalize_pack
  - pack_manifest.json -> JSON schema contract

## Notes
- Contract source: `contracts/contract-rules.yaml`
- Workflow: {{workflowId}}
- Run: {{runId}}
